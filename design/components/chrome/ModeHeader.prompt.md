Top-of-panel row: 11px mode label at left, `ConnectionMark` at right.

```jsx
<ModeHeader mode="NAV" connected heartbeat />
```

Always present so a glance confirms both "what screen is this" and "is the phone still talking to me" in the same 0.1s.
