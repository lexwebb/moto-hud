Thin full-width rule used to separate chrome rows from content.

```jsx
<PixelDivider variant="dashed" />
```

Three variants: `solid` (default), `dashed` (softer separation), `dither` (checkerboard pattern — this system's substitute for gray/opacity, since 1-bit e-ink has no gray).
