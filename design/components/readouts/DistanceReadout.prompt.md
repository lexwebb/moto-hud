The hero number on the Nav screen — distance to the next maneuver.

```jsx
<DistanceReadout value="350" unit="m" size="hero" />
```

Unit (m/km) rides as a small suffix glyph, not a settings toggle — "compact unit switching as typography." Use `size="secondary"` at 17px for less-important numeric rows (e.g. ETA).
