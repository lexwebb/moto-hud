# Exported meshes for print + browser preview.

- Bench clamshell: `base` / `lid` / `caps` / `assembly` from `../moto_hud_case.scad`
- Dock slabs: `dock_interface` / `dock_plate_slab` / `dock_pod_slab` / `dock_plate_usb` from `../dock_interface.scad`

Regenerate with `../export_meshes.sh` (see `../README.md`).
